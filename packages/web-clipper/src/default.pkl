amends "@obsidian.webClipper/Template.pkl"

import "../props.pkl"

name = "Default"

behavior = "create"

noteContentFormat =
  """

  {{content}}

  """

properties = props.common.toMap().values

triggers = List()

noteNameFormat = "{{title}} - {{domain}}"

path = "Clippings"
